# DataScript Test Port Ledger

This tracks direct ports from `../datascript/test/datascript/test/*.cljc`.
See [datascript-test-matrix.md](datascript-test-matrix.md) for the broader
namespace classification and batch order.

`subset` means Vev has local tests for the main behavior, but not every
DataScript assertion or exact Clojure API shape.

Because Vev's primary long-term consumers are non-Kvist callers, a port should
prefer EDN/text or prepared API coverage before Kvist literal-only coverage.
Literal macro tests are useful, but they do not by themselves close a DataScript
compatibility gap.

| Namespace | Upstream deftests | Vev status | Next gap |
| --- | ---: | --- | --- |
| `query.cljc` | 11 | covered | basic joins, inputs, bindings, map-form text queries, nested `:in` binding patterns, ignored `_` input placeholders in literal and text queries, DB-backed tx/op data pattern terms, relation source joins, primary/named collection datom sources including 1-wide scalar rows, 2-wide tuple rows, primary map relation sources, long `[e a v tx op]` rows, typed relation/collection/map input values, and prepared primary collection queries, direct map-as-relation inputs and nested map values, checked scalar/collection/tuple/relation input shape errors, multi-DB source joins, constants including symbols, placeholders, and issue-385-style zero-arg/native function input behavior covered; exact Clojure diagnostic text is host-shaped |
| `query_find_specs.cljc` | 1 | covered | Vev result helpers cover collection, tuple, scalar, cut, and aggregate find specs |
| `query_aggregates.cljc` | 1 | subset | built-in aggregates including numeric sum/avg, multi-argument aggregate tuple distinctness, and DataScript-shaped `(aggregate ?f ?x)` with named native aggregate functions plus typed aggregate callbacks through text/prepared APIs covered; exact edge behavior remains |
| `query_not.cljc` | 5 | covered | nested `not`, `not-join`, relation and DB source-prefixed forms, nested inherited/override source, multi-relation anti-join edge cases, checked unbound-group errors, query text plus Kvist literal macro source-order top-level `not` validation, and supported ordered rule bodies covered; exact diagnostics are Vev-shaped |
| `query_or.cljc` | 5 | covered | relation and DB source-prefixed forms, nested inherited/override source, plain-`or` branch var matching, nested `or-join` binding forms, relation-source holes/mixed-width joins, constant substitution regressions, and `or-join` projection validation covered; exact diagnostics are Vev-shaped |
| `query_pull.cljc` | 8 | covered | basic, wildcard, variable-pattern including bare pattern names, text pattern-variable, aggregate, lookup-ref pull, multi-source pull including `$` default source behavior, and scalar/collection/tuple pull find-spec shapes covered; DataScript's disabled multi-pattern test remains unsupported upstream too |
| `query_rules.cljc` | 3 | subset | source-qualified relation rule calls, repeated rule calls, fixpoint/symmetric recursion, wildcard rule-call arguments, literal/text rule body source order, inline map-query `:rules` through text/prepared APIs, primary collection DB rule queries, native predicate/function inputs including DB-aware predicates inside rule bodies, DataScript issue-456-style duplicate rule propagation coverage with intermediate binding dedup, DataScript rule-vars required-argument syntax for literal/text rules, duplicate rule var validation, branch arity validation, and unknown/arity/param validation covered; full semi-naive/measured performance remains |
| `query_fns.cljc` | 7 | covered | built-in predicates/functions including boolean/nil predicates, string contains/prefix/suffix helpers, integer arithmetic helpers, `name`, `str`, `keyword`, `subs`, `range`, regex predicate and match-return `re-pattern`/`re-find`, scalar/collection/tuple/relation `ground` including EDN text parsing, `get-else` EDN text parsing plus lookup-ref inputs and nil-default rejection, `get-some` EDN text parsing plus lookup-ref inputs, nested result binding including EDN vector/map function args and text/prepared tuple/relation/collection output destructuring, result unification, lookup-ref inputs, named predicate/function operator inputs over supported built-ins, registered native-op aliases, typed native predicate/value callbacks, DB-aware native predicates, unresolved data-bound predicate values, rule/function binding interaction, and checked query errors covered; exact Clojure diagnostic text is host-shaped |
| `query_return_map.cljc` | 1 | covered | Vev uses keyed rows with keyword/string/symbol key kinds instead of Clojure maps |
| `lookup_refs.cljc` | 5 | covered | entity/query/tx/index lookup refs, mixed entity-id collection inputs, scalar and collection lookup-ref query inputs with DataScript-style projection of the original lookup-ref value, multi-source lookup-ref inputs, unresolved inline query lookup-ref errors, missing lookup-ref retract no-ops, and checked missing/non-unique lookup-ref errors covered; exact host/API rendering is Vev-shaped |
| `ident.cljc` | 4 | covered | Vev returns ref values as `Value.Entity`; behavior is otherwise covered |
| `explode.cljc` | 4 | covered | cardinality-many tx-map collection expansion, ref collections, reverse ref tx-map scalar/lookup/tempid values, nested maps, multi-valued nested maps including EDN set values, reverse nested maps, circular/nested ref shapes, and reverse-attr ref-schema validation covered; Clojure host list/array inputs are not part of Vev's native API surface |
| `entity.cljc` | 6 | covered | entity id/db access, scalar/many attr reads, contains, lookup-ref and ident construction, missing/dangling refs, forward and reverse ref navigation including nested reverse refs, and touch are covered; Clojure equality/hash/print/cache protocol semantics are host |
| `pull_api.cljc` | 17 | subset | repeated attr-spec normalization, DataScript namespaced reverse attrs, reverse component pull id/pattern/recursion shapes, plain rendered `Value.Map`/`Value.Vector` pull result shape including keyword/string/symbol `:as` keys, named `:xform` transforms for `vector`/`name`, typed native pull xform callbacks through text/prepared/query pull APIs, and DataScript-shaped typed visitor trace events for attr/wildcard/reverse/nested pull traversal covered; final C ABI streaming callback shape and exact collection/scalar render edge cases remain |
| `components.cljc` | 2 | covered | component schema validation, component `retractEntity`, `retractAttribute`, forward pull/touch expansion, multival components, and reverse navigation are covered; exact Clojure entity/touch render shapes are host |
| `transact.cljc` | 19 | subset | DataScript-style report `:db/current-tx` tempids, current-tx value aliases for `datomic.tx`/`datascript.tx` resolved to the actual report tx id, datom transaction/replay reports preserving explicit tx/op fields plus listener delivery, forward/cyclic tempid ref values including generated-id map parents, EDN text/prepared negative numeric tempids and single operation vectors, schema-aware map vector/set values for cardinality-many attrs versus tuple/scalar attrs, reverse ref tx-map values and nested maps through EDN text, map-valued datom compare/lookup, type-sensitive numeric retract, mixed-type unique lookup/index compare, adjacent retractEntity tx-data reporting, retract not-found/idempotency, unused value-tempid rejection including empty cardinality-many definitions, tempids-outside-add rejection, large entity-id rejection, native transaction functions, registry-backed ident tx functions including EDN text/prepared `:db.fn/call` and shorthand ident calls with zero or more typed value args, tx fns returning new tempid entities and generated map entities without `:db/id`, and `transact-text` EDN string interop covered; exact parser/API diagnostics and any still-unported transaction edge assertions remain |
| `upsert.cljc` | 6 | covered | unique cardinality-many, multi-identity convergence, unique-value no-upsert, vector tx tempid ordering, DataScript retry-order tempid merging, redefining tempids, current-tx and explicit-id identity conflicts, ref and lookup-ref upserts, forward string/negative tempid refs, non-upsert of new ref tempids, and EDN text/prepared transaction coverage for ordinary identity upsert, two-identity upsert, lookup-ref upsert, retry convergence, forward ref tempids, and rollback on conflict covered; exact diagnostic text is Vev-shaped |
| `db.cljc` | 4 | covered | DB diff and datom/index API compatibility covered; hash/cache/uuid/record behavior is host |
| `index.cljc` | 5 | covered | main order, component filtering, AVET schema filtering/errors, empty DB `find-datom`, `find-datom` prefixes, seek/rseek/range, sequence compare, lookup-ref index args, unresolved lookup-ref errors, and sorted-index binary bounds covered; exact lazy sequence/API rendering details are host/API shape |
| `tuples.cljc` | 11 | covered | tuple value/component upsert, tuple lookup-ref queries including EDN text queries and nested ref lookup refs, tuple lookup-ref pull via literal/text/prepared APIs, tuple lookup-ref unique conflict/update paths through EDN text tx-data, multi-component unique updates, DataScript's vector-sequential versus tx-map-atomic tuple component update behavior, direct tuple attr add/retract validation, redundant direct tuple-value ignore cases from map and vector tx-data including text tx-data, final-state direct tuple update assertions, stale explicit direct tuple update rejection, inferred tuple value type/cardinality metadata, final tuple schema validation independent of tx-data order, EDN text/prepared tuple component upsert, tuple-value tempid upsert, tuple lookup-ref entity refs, conflict rollback, invalid tuple schema shapes, tuple indexes, and tuple query functions covered; exact diagnostics are Vev-shaped |
| `validation.cljc` | 2 | covered | runtime and EDN text validation cover bad entity ids, bad attrs, nil values, bad ref values, unknown ops, tempids outside add, bad tx root shapes, uniqueness, schema booleans/keywords, component schema, symbol value type validation, and rollback/no-mutation; exact Clojure exception types/messages are host |
| `parser*.cljc` | 19 | partial | `parser_find`, `parser_query`, `parser_return_map`, `parser_where`, and `pull_parser` are covered through EDN text/prepared APIs, and direct `parser.cljc` binding/`:in`/`:with` parser APIs cover scalar vars, `_`, collection, tuple, nested collection/tuple bindings, source vars, and `%`. Direct `parse-rules-text` and query text paths cover rule definitions, plain and required rule vars, multiple branches, source-qualified rule calls, ordered rule body steps, duplicate rule var checks, empty branch checks, and branch arity checks. Query text parses vector and map query forms including inline map-query `:rules`, scalar/collection/tuple/pull/aggregate/custom-aggregate/top-n aggregate find specs including multi-argument aggregate elements with constants/source-symbol args, pull pattern variables including bare pattern names, flat and nested pull option forms including named `:xform`, repeated pull attr specs, list-valued nested pull map patterns, option-wrapped pull map keys, `:with`, return-map markers for keyed text helpers with marker-specific DataScript-style duplicate/count/scalar/collection validation, scalar/collection/tuple/relation/relation-source/named-DB-source `:in`, ignored `_` input placeholders, direct scalar map values for relation bindings, float literals, EDN set literals as collection values, nested input/result destructuring, source-order binding for mixed input kinds and top-level `:where`, optional `:where`, data clauses with symbol constants, `$`-prefixed symbol constants in non-source positions, lookup-ref entity terms, DataScript namespaced reverse attrs, and 1/2/3/4/5-wide relation source patterns, zero-arg and normal predicate/function forms, `ground`, `get-else`, `get-some`, missing?, top-level/rule-body and nested `and`, not/not-join, or/or-join including source-qualified text groups, and ordinary/source-qualified rule calls with wildcard arguments and rule-body `get-else`/`get-some`. Remaining parser partials are exact parser object/API rendering and exact diagnostic text |
| `query_v3.cljc` | 2 | covered | active validation cases plus the disabled relation-query example are covered through Vev relation DB APIs, including arity, non-collection sources, explicit `$`, prepared queries, and scalar/tuple/map primary sources |
| `conn.cljc` | 2 | covered | create/conn-from-db/conn-from-datoms plus reset reports with before/after snapshots, tx-data, metadata, and listener delivery are covered; Clojure schema-map field shape is host |
| `serialize.cljc` | 5 | covered | init-db from datoms plus typed serializable and EDN-ish datom snapshot text roundtrips covered, including schema, retractions, refs, symbol/map/vector values, special floats, and next-tx recovery; DataScript's JVM/CLJS codec matrix is host/format work |
| `filter.cljc` | 1 | covered | materialized `filter-db` covers predicate filters, chaining, entity reads, index-backed queries, and semantic DB equivalence; Clojure hash/equality/runtime wrapper behavior is host |
| `listen.cljc` | 1 | covered | named report-sink listeners cover registration, tx-data reports, metadata reports, and unlisten; arbitrary closure callbacks are host/ABI design work |
| `issues.cljc` | 5 | covered | issue-262 vector result isolation, issue-369 mixed-type DB diff, and issue-381 schema inspection covered; issue-330/331 are Clojure pprint/meta host behavior |
| `storage.clj`, `datafy.cljc` | 6 | later | app/runtime APIs after core parity |

Near-term rule: port one namespace at a time, and only mark `covered` when the
remaining differences are intentional non-Clojure API shape differences.
