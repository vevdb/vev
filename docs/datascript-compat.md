# DataScript Compatibility

Vev has not ported the DataScript test suite yet. This file tracks the local
`../datascript/test/datascript/test/*.cljc` namespaces as the compatibility
checklist.

See `docs/datascript-test-port-ledger.md` for namespace-by-namespace port
status.

Status key:

- `covered`: Vev has comparable tests and behavior
- `partial`: Vev supports a useful subset
- `missing`: not implemented yet
- `later`: likely outside the near-term in-memory compatibility target

## Current Map

| DataScript area | Status | Notes |
| --- | --- | --- |
| `query` | partial | DataScript join subset ported including existence and multi-hop joins; inputs, no-where input-only queries, empty collection/relation inputs, plain int entity inputs, collection/tuple/relation bindings, direct map-as-relation inputs, typed relation/collection/map inputs as named relation sources, checked scalar/collection/tuple/relation input shape errors, cardinality-many joins, DB-backed data patterns with tx/op terms, named relation source joins, primary/named collection datom sources including 1-wide scalar rows, 2-wide tuple rows, long `[e a v tx op]` rows, prepared primary collection queries, multi-DB source joins, `_` input placeholders, symbol constants, and constant-substitution subset covered |
| `query_aggregates` | partial | Relation-input subset ported; grouped aggregates, `:with` duplicate preservation, numeric sum/avg including fractional averages and float values, keyword min/max comparator subset, multi-argument aggregate tuple distinctness in literal/text APIs, literal top-n min/max, parameterized top-n min/max, and DataScript-shaped `(aggregate ?f ?x)` with named native aggregate functions and typed aggregate callbacks covered; exact edge behavior incomplete |
| `query_find_specs` | partial | Subset ported; scalar, collection, tuple, aggregate find specs, and multiple-result cuts covered |
| `query_fns` | partial | DataScript subset ported for predicates including `even?`, `odd?`, `zero?`, `pos?`, `neg?`, `true?`, `false?`, `nil?`, `some?`, `not`, `empty?`, string contains/prefix/suffix predicates, mixed-type predicate comparison, scalar/collection/tuple/relation `ground` including EDN text parsing, `get-else` including EDN text parsing, lookup-ref entity inputs, and nil-default rejection, `get-some` including vector/map EDN text parsing and lookup-ref entity inputs, `missing?`, literal and EDN text vector/map function args, tuple construction/destructuring, function-result unification with vector `first`/`second`, text/prepared output destructuring over tuple/relation/collection bindings, named predicate/function operator inputs over supported built-ins in literals and EDN text, registered native-op aliases and typed native predicate/value callbacks for EDN/prepared text predicates/functions including rule bodies and primary collection DB queries, rule/function binding interaction, regex predicates and match-return clauses via `re-pattern`/`re-find`, and built-in result clauses for `identity`, `inc`, `dec`, `range`, `name`, `str`, `keyword`, `subs`, `vector`, `tuple`, `untuple`, `hash-map`, `get`, string `count`, integer arithmetic, `quot`, `rem`, `mod`, `min`, `max`, and `compare`; exact Clojure error behavior remains |
| `query_not` | partial | DataScript subset ported, including nested `not`, explicit relation/DB sources inside `not`, direct source-prefixed `($src not ...)`, explicit source override inside source-prefixed `not`, nested inherited/override source for `not`, and checked errors for groups with no positive binding; query text and Kvist literal macros now preserve source-order binding validation for top-level `not`, and supported rule bodies execute in source order |
| `query_or` | partial | DataScript subset ported, including scalar-input `or`, direct source-prefixed `($src or ...)`, explicit source override inside source-prefixed `or`, nested default-source inheritance and nested source override for `or`, relation-source `or-join`, nested `or-join` binding forms, checked plain-`or` branch var matching, and `or-join` projection validation; exact Clojure diagnostics remain |
| `query_pull` | partial | Basic literal-pattern, `:in` variable-pattern including DataScript-style bare pattern names, returning pattern values, lookup-ref input, multi-source pull, pull-with-aggregates, and scalar/collection/tuple pull find-spec DataScript subsets ported; exact Clojure return rendering remains |
| `query_return_map` | partial | DataScript subset ported for all-row `:keys`/`:strs`/`:syms` and tuple return maps |
| `query_rules` | partial | Literal and text rule input subsets started: rule branches plus fixpoint recursive/mutual/symmetric rules with data clauses, repeated rule calls, inline map-query `:rules` through text/prepared APIs, primary collection DB rule queries, ordered body execution for data/predicate/function/ground/rule-call/not/or steps, predicates including `even?`, `ground`, built-in function clauses, false arguments, source-qualified relation rule calls, intermediate rule-binding dedup for DataScript issue-456-style duplicate propagation, DataScript rule-vars required-argument syntax, duplicate rule var checks, branch arity checks, and validation for unknown rules, bad arity, and malformed rule params; host predicate inputs and full semi-naive performance work incomplete |
| `parser_*` | partial | Text APIs are backed by the flat EDN reader. Query text parses `[:find ... :with ... :in ... :where ...]` and map query forms with inline `:rules`, scalar, collection, tuple, pull, aggregate including multi-argument aggregate elements, custom aggregate, and top-n aggregate find specs, keyed return-map markers including count/shape validation, scalar/collection/tuple/relation/relation-source/named-DB-source inputs including direct scalar map values for relation bindings, data clauses including lookup-ref entity terms, float constants, EDN set constants as collection values, DataScript namespaced reverse attrs such as `:user/_friend`, and 1/2/3/4/5-wide relation-source rows, predicate clauses, built-in function clauses with EDN vector/map/set literal args, `ground`, `get-else`, `get-some`, `missing?`, top-level/rule-body `and`, rule-body `get-else`/`get-some`, `not`/`not-join`, `or`/`or-join`, ordinary/source-qualified rule calls and definitions including rule-vars required-argument vectors, no-`:where` input-only queries, bare-symbol pull pattern inputs, and ordered top-level `:where` execution/validation. Kvist literal query macros now lower top-level `:where` forms with the same ordered step metadata and rule-vars metadata. Query validation now covers unknown `:find`/`:with` vars, `:find`/`:with` overlap, duplicate variable inputs, duplicate `:with` vars, unknown sources, missing `%` for rule calls, malformed not/or/rule parser shapes, and empty join-var sets. Pull text APIs parse EDN pull vectors directly, including list-valued nested map patterns and named `:xform` options. Transaction text parses common tx-data vectors/maps/sets including float values, single operation vectors, DataScript-style negative numeric tempids in entity/ref positions, preserves map vector/set values for tuple/cardinality-one attrs while expanding cardinality-many collections, expands reverse ref tx-map attrs including nested maps, and rejects bad root shapes, bad ops, bad attrs, nil values, bad entity ids, and tempids outside add before mutating; exact validation remains |
| `pull_api` | partial | Attrs, wildcard, reverse refs, nesting, nested ref-map filtering, pull-many, string/false/ref defaults, reverse-ref defaults, default/numeric/nil limits, repeated attr-spec normalization, bounded recursion, capped `...` recursion, component expansion, reverse component id/pattern/recursion shapes, component map override examples, ABI-friendly named `:xform` transforms for `vector`/`name`, and typed native pull xform callbacks through text/prepared/query pull APIs are covered; visitors, final C ABI registration shape, and exact rendered collection/scalar shapes remain |
| `pull_parser` | partial | Kvist pull literals, query text pull finds, and direct `pull-text` APIs cover attrs, wildcard, DataScript namespaced reverse attrs, nested map patterns with vector or list pattern values, recursive map values, flat/nested `:default`/`:as`/`:limit`/`:xform` option vectors, repeated attr specs, option-wrapped map keys, malformed pattern rejection, and schema-aware query validation for reverse refs, nested refs/recursion, and explicit limits; exact diagnostics remain |
| `transact` | partial | Add/retract/map forms, nil item skipping, value-specific retract no-ops including int-vs-float exactness, retract not-found/idempotency, mixed-type unique lookup/index compare, incoming-ref cleanup and adjacent tx-data reporting on retractEntity, ref-typed numeric value resolution, DataScript-style report `:db/current-tx` tempids, forward/cyclic tempid ref values including generated-id map parents, DataScript-style negative numeric tempids through EDN text/prepared tx-data, map-valued datom compare/lookup subset, schema-aware map vector values for cardinality-many attrs versus tuple/scalar attrs, unused value-tempid rejection including empty cardinality-many map definitions, intermediate-DB lookup/CAS resolution subset, cardinality-one/unique/default replacement, CAS one/many/nil/lookup-ref value subset, tempids-outside-add rejection, native transaction functions returning tx-data, registry-backed ident transaction functions via DataScript-shaped tx-data vectors plus EDN text/prepared `:db.fn/call` and ident shorthand forms, and `transact-text` EDN string interop for common vector/map tx-data, float values, plus single operation vectors covered; arbitrary Clojure function values and exact errors incomplete |
| `explode` | partial | DataScript tx-map explode semantics are covered for cardinality-many scalar/ref collections, reverse ref tx-map scalar/lookup/tempid values, nested maps, multi-valued nested maps including EDN sets, reverse nested maps, and circular/nested ref shapes; host list/array inputs and exact reverse-attr schema diagnostics remain |
| `upsert` | partial | Unique-identity map/list tempid upsert, multi-identity convergence, vector tx tempid ordering, DataScript retry-order tempid merging, current-tx conflict, unique-value no-upsert enforcement, intermediate-db retry subset, string tempid refs including forward refs, non-upsert of newly allocated ref tempids, unique-ref numeric/lookup-ref upsert, conflicting unique-field subsets, and EDN text/prepared transaction coverage for ordinary identity upsert, lookup-ref upsert, retry convergence, forward ref tempids, and rollback on conflict covered; exact conflict messages incomplete |
| `lookup_refs` | partial | Lookup refs covered in query entity/value positions, scalar and collection query inputs including mixed entity-id collections and multi-source joins, unresolved inline query lookup-ref errors, pull, tx entity/value/map-value/CAS entity/expected/value positions, nested tuple values, missing-ref retract no-ops, and `datoms`/`seek-datoms`/`index-range` index access including checked unresolved lookup-ref errors; exact Clojure invalid lookup-ref errors remain incomplete |
| `ident` | partial | DataScript query/transact/pull subset, entity ident lookup, and missing-ident retract no-ops covered |
| `components` | partial | Component schema requires ref attrs, component `retractEntity`, `retractAttribute`, incoming-ref cleanup, forward pull expansion, reverse pull id/pattern/recursion subsets, and entity `touch` covered; exact Clojure render/protocol shapes remain |
| `entity` | partial | Basic entity view API covered: id/db access, lookup-ref and ident construction, scalar/many attr reads, forward and reverse ref navigation, contains, missing entities, and touch; Clojure map protocol/cache/print/equality semantics do not apply directly in Kvist |
| `index` | partial | `datoms`, `find-datom` including empty DB and prefix lookups, `seek-datoms`, `rseek-datoms`, upstream `index-range` bounds/order examples, public AVET filtering, sequence-value compare cases, and DataScript-shaped indexed-attribute plus unresolved lookup-ref errors covered; public access now uses binary lower/upper bounds over sorted in-memory indexes instead of whole-index scans; remaining gaps are exact lazy sequence/API rendering details |
| `validation` | partial | Nil value rejection plus value type including `:db.type/symbol`, uniqueness, component schema, ident values, schema boolean attrs, schema keyword enum attrs, cardinality, unknown tx op, bad attrs, bad lookup attrs, tempids-outside-add, and `transact-text` bad-shape rollback subsets covered; remaining reader/macro bad forms and exact validation errors incomplete |
| `db` | partial | Immutable DB values, current datom indexes, public datom access, and semantic DB diff covered; Clojure hash/cache/record/uuid helpers are host/runtime-specific |
| `conn` | partial | `conn-from-db`, `conn-from-datoms`, reset reports, and named report-sink listeners covered; arbitrary callback/closure API remains |
| `filter` | partial | Materialized `filter-db` covers DataScript-style predicate filtering, chained filters, filtered entity reads, and index-backed queries; exact hash/equality/runtime wrapper behavior remains |
| `serialize` | partial | `init-db` from datoms and typed EDN-ish datom snapshot text roundtrip covered, including schema, retractions, refs, symbol/map/vector values, and next-tx recovery; JSON/transit-style formats later |
| `issues` | partial | Engine-relevant regressions from `issues.cljc` are triaged: vector result isolation, mixed-type DB diff, and schema inspection covered; Clojure metadata/pprint cases are host-specific |
| `listen`, `storage`, `datafy` | later | API/package features after semantic core |
| `tuples` | partial | Tuple attr schema, inferred tuple value type/cardinality metadata, final tuple schema validation independent of tx-data order, derived multi-tuple transaction maintenance, direct tuple attr rejection except redundant final-state tuple assertions, stale explicit direct tuple update rejection, unique tuple lookup refs including ref-component nested lookup refs, query lookup refs over tuple attrs in literal and EDN text APIs, tuple lookup-ref pull via literal/text/prepared APIs, tuple lookup-ref unique conflict/update paths through EDN text tx-data, component-based tuple upsert, direct tuple-value tempid upsert, tuple unique conflict and multi-component update shapes, EDN text/prepared tx-data tuple component upsert, tuple lookup-ref entity refs, prepared direct tuple-value tempid upsert, conflict rollback, text/map tx-data direct tuple-value ignore cases, public AVET/index-range without explicit `:db/index`, tuple query functions, tuple type/attrs validation including invalid `:db/tupleAttrs` shapes, nested tuple dependency rejection, and cardinality-many tuple/component rejection subsets; remaining exact errors and edge conflict matrix missing |

## Next Porting Order

The local compatibility suite currently passes 330 tests. The remaining
DataScript work should focus on the areas where Vev still differs in engine
semantics or required native interop, not on already-covered syntax families.

1. Finish tuple compatibility: the remaining useful work is the edge conflict
   matrix and exact schema validation behavior, especially through EDN
   text/prepared transaction APIs.
2. Tighten parser validation against the upstream parser namespaces. Query,
   pull, rule, return-map, and transaction text parsing are broad now, but
   malformed-shape handling still trails DataScript.
3. Extend the native callback ABI beyond query predicate/value/aggregate/pull
   xform callbacks: transaction functions, listeners, and the final
   C ABI-facing registration shape remain.
4. Add measurement-driven performance work for recursive rules, large
   relations, aggregates, and index-backed planning before starting durable
   storage.
