# DataScript Compatibility

Vev has not ported the DataScript test suite yet. This file tracks the local
`../datascript/test/datascript/test/*.cljc` namespaces as the compatibility
checklist.

See `docs/datascript-test-port-ledger.md` for namespace-by-namespace port
status.

Status key:

- `covered`: Vev has comparable tests and behavior
- `partial`: Vev supports a useful subset
- `missing`: not implemented yet
- `later`: likely outside the near-term in-memory compatibility target

## Current Map

| DataScript area | Status | Notes |
| --- | --- | --- |
| `query` | covered | DataScript join semantics covered including existence and multi-hop joins; inputs, no-where input-only queries, empty collection/relation inputs, plain int entity inputs, collection/tuple/relation bindings, direct map-as-relation inputs, typed relation/collection/map inputs as named relation sources, checked scalar/collection/tuple/relation input shape errors, cardinality-many joins, DB-backed data patterns with tx/op terms, named relation source joins, primary/named collection datom sources including 1-wide scalar rows, 2-wide tuple rows, primary map relation sources, long `[e a v tx op]` rows, prepared primary collection queries, portable text/prepared result `Value` rendering including rules and named DB sources, portable profiled text/prepared query result rendering with candidate/rule counters, multi-DB source joins, ignored `_` input placeholders in literal and text queries, nested binding patterns, symbol constants, constant substitution, and issue-385-style zero-arg/native function input behavior covered through EDN text/prepared-compatible registered functions; exact Clojure diagnostic text is host-shaped |
| `query_aggregates` | covered | Relation-input aggregate semantics covered: grouped aggregates, `:with` duplicate preservation, numeric sum/avg including fractional averages and float values, median, variance, stddev, keyword min/max comparator ordering, multi-argument aggregate tuple distinctness in literal/text APIs, literal top-n min/max, parameterized top-n min/max, and DataScript-shaped `(aggregate ?f ?x)` with named native aggregate functions and typed aggregate callbacks; exact diagnostic text is Vev-shaped |
| `query_find_specs` | covered | Scalar, collection, tuple, aggregate find specs, and multiple-result cuts covered |
| `query_fns` | covered | DataScript function and predicate semantics covered for `even?`, `odd?`, `zero?`, `pos?`, `neg?`, `true?`, `false?`, `nil?`, `some?`, `not`, `empty?`, string contains/prefix/suffix predicates, mixed-type predicate comparison, scalar/collection/tuple/relation `ground`, `get-else`, lookup-ref entity inputs, nil-default rejection, `get-some`, `missing?`, literal and EDN text vector/map function args, tuple construction/destructuring, function-result unification with vector `first`/`second`, text/prepared output destructuring over tuple/relation/collection bindings, named predicate/function operator inputs over supported built-ins, registered native-op aliases, typed native predicate/value callbacks, DB-aware native predicates, unresolved data-bound predicate values, rule/function binding interaction, regex predicates and match-return clauses via `re-pattern`/`re-find`, and built-in result clauses for `identity`, `inc`, `dec`, `range`, `name`, `str`, `keyword`, `subs`, `vector`, `tuple`, `untuple`, `hash-map`, `get`, string `count`, integer arithmetic, `quot`, `rem`, `mod`, `min`, `max`, and `compare`; exact Clojure diagnostic text is host-shaped |
| `query_not` | covered | DataScript `not`/`not-join` semantics covered, including nested `not`, explicit relation/DB sources inside `not`, direct source-prefixed `($src not ...)`, explicit source override inside source-prefixed `not`, nested inherited/override source for `not`, implementation edge cases with multi-relation anti-joins, and checked errors for groups with no positive binding; query text and Kvist literal macros preserve source-order binding validation for top-level `not`, and supported rule bodies execute in source order |
| `query_or` | covered | DataScript `or`/`or-join` semantics covered, including scalar-input `or`, direct source-prefixed `($src or ...)`, explicit source override inside source-prefixed `or`, nested default-source inheritance and nested source override for `or`, relation-source `or-join` with holes and mixed-width sources, nested `or-join` binding forms, constant substitution regressions, checked plain-`or` branch var matching, and `or-join` projection validation; exact diagnostic text is Vev-shaped |
| `query_pull` | covered | Basic literal-pattern, wildcard pulls, scalar values interleaved with pull results, `:in` variable-pattern including DataScript-style bare pattern names, returning pattern values, lookup-ref input, multi-source pull with `$` default source behavior, pull-with-aggregates, and scalar/collection/tuple pull find-spec DataScript subsets covered; DataScript's disabled multi-pattern test remains unsupported upstream too |
| `query_return_map` | covered | Vev keyed rows cover all-row `:keys`/`:strs`/`:syms` and tuple return-map shapes; exact Clojure map rendering is host/API shape |
| `query_rules` | partial | Literal and text rule semantics covered: rule branches plus fixpoint recursive/mutual/symmetric rules with data clauses, repeated rule calls, wildcard rule-call arguments, inline map-query `:rules` through text/prepared APIs, reusable prepared query plus prepared rules APIs for DB and primary relation DB queries with native function callbacks, primary collection DB rule queries, ordered body execution for data/predicate/function/ground/rule-call/not/or steps, predicates including `even?`, `ground`, built-in function clauses, native predicate/function inputs including DB-aware predicates inside rule bodies, false arguments, source-qualified relation rule calls, intermediate rule-binding dedup for DataScript issue-456-style duplicate propagation, direct leaf-rule evaluation for upstream issue-456-style larger datasets, DataScript rule-vars required-argument syntax, duplicate rule var checks, branch arity checks, validation for unknown rules, bad arity, and malformed rule params, plus profiled rule-call/fixpoint iteration counters; full semi-naive recursive optimization remains |
| `parser_*` | partial | `parser_find`, `parser_query`, `parser_return_map`, `parser_where`, and `pull_parser` are covered through EDN text/prepared APIs, and direct parser APIs cover binding/`:in`/`:with` plus rule definitions for plain vars, required rule vars, multiple branches, source-qualified rule calls, ordered rule body steps, and empty/duplicate/arity rule validation. Query text parses `[:find ... :with ... :in ... :where ...]` and map query forms with inline `:rules`, scalar, collection, tuple, pull, aggregate including multi-argument aggregate elements with constants/source-symbol args, custom aggregate, and top-n aggregate find specs, keyed return-map markers including marker-specific duplicate/count/collection/scalar validation, scalar/collection/tuple/relation/relation-source/named-DB-source inputs including ignored `_` placeholders and direct scalar map values for relation bindings, data clauses including symbol constants, `$`-prefixed symbol constants in non-source positions, lookup-ref entity terms, float constants, EDN set constants as collection values, DataScript namespaced reverse attrs such as `:user/_friend`, and 1/2/3/4/5-wide relation-source rows, predicate clauses, built-in function clauses with EDN vector/map/set literal args, `ground`, `get-else`, `get-some`, `missing?`, top-level/rule-body `and`, rule-body `get-else`/`get-some`, `not`/`not-join`, `or`/`or-join`, source-qualified `($src not ...)`/`($src not-join ...)`/`($src or ...)`/`($src or-join ...)` groups, ordinary/source-qualified rule calls with wildcard arguments and definitions including rule-vars required-argument vectors, no-`:where` input-only queries, bare-symbol pull pattern inputs, and ordered top-level `:where` execution/validation. Prepared binding/`:in`/`:with`/rules/query/tx-data parser results have portable EDN-ish `Value.Map` rendering. Remaining parser partials are exact Clojure parser record rendering and exact diagnostic text |
| `pull_api` | partial | Attrs, wildcard, reverse refs, nesting, nested ref-map filtering, pull-many, string/false/ref defaults, reverse-ref defaults, default/numeric/nil limits, repeated attr-spec normalization, bounded recursion, unbounded `...` recursion semantics with upstream-shaped 3000-hop deep recursion coverage, component expansion, reverse component id/pattern/recursion shapes, component map override examples, pull over filtered/restored/`init-db` DB values, plain rendered `Value.Map`/`Value.Vector` result shape including keyword/string/symbol `:as` keys and nil pull-many lookup-ref misses, text/prepared/lookup-ref pull APIs that return portable `Value` roots, ABI-friendly named `:xform` transforms for `vector`/`name`, typed native pull xform callbacks through text/prepared/query pull APIs, DataScript-shaped typed visitor trace events for attr/wildcard/reverse/nested pull traversal, and C ABI result-row/value-tree callbacks for pull/result values are covered; exact rendered edge cases remain |
| `pull_parser` | covered | Kvist pull literals, query text pull finds, and direct `pull-text` APIs cover attrs, wildcard including string `"*"`, DataScript namespaced reverse attrs, nested map patterns with vector or list pattern values, recursive map values including string ellipsis, flat/nested `:default`/`:as`/`:limit`/`:xform` option vectors, string/vector option heads, repeated attr specs, option-wrapped map keys, malformed pattern rejection, and schema-aware query validation for reverse refs, nested refs/recursion, and explicit limits; exact parser object rendering/diagnostic text is Vev-shaped |
| `transact` | partial | Add/retract/map forms, nil item skipping, value-less attr retracts including cardinality-many removal, value-specific retract no-ops including boolean mismatch and int-vs-float exactness, retract not-found/idempotency, mixed-type unique lookup/index compare, incoming-ref cleanup and exact adjacent tx-data reporting on retractEntity, ref-typed numeric value resolution, DataScript-style report `:db/current-tx` tempids, current-tx value aliases for `datomic.tx`/`datascript.tx` resolved to the actual report tx id even when user entity ids are higher, portable transaction report `Value` rendering for text/prepared/immutable APIs, forward/cyclic tempid ref values including generated-id map parents, DataScript-style negative numeric tempids through EDN text/prepared tx-data, map-valued datom compare/lookup subset, schema-aware map vector values for cardinality-many attrs versus tuple/scalar attrs, unused value-tempid rejection including empty cardinality-many map definitions, intermediate-DB lookup/CAS resolution subset, cardinality-one/unique/default replacement, CAS one/many/nil/lookup-ref value subset including DataScript-shaped failed-datom diagnostics, tempids-outside-add rejection, native transaction functions returning tx-data including generated map entities without `:db/id`, registry-backed ident transaction functions via DataScript-shaped tx-data vectors plus EDN text/prepared `:db.fn/call` and ident shorthand forms with zero or more typed value args, immutable DB-value `with-*`/`db-with-*` APIs for text/prepared EDN tx-data including registered tx functions, transaction functions returning new tempid entities, failed registered transaction functions rolling back earlier segment ops without bumping tx or notifying listeners across literal/text/prepared APIs, and `transact-text` EDN string interop for common vector/map tx-data, float values, plus single operation vectors covered; exact parser/API diagnostics and any still-unported transaction edge assertions remain |
| `explode` | covered | DataScript tx-map explode semantics are covered for cardinality-many scalar/ref collections, reverse ref tx-map scalar/lookup/tempid values, nested maps, multi-valued nested maps including EDN sets, reverse nested maps, circular/nested ref shapes, and reverse-attr ref-schema validation; Clojure host list/array inputs are not part of Vev's native API surface |
| `upsert` | covered | Unique-identity map/list/tempid upsert, two-identity convergence, vector tx tempid ordering, DataScript retry-order tempid merging, current-tx conflict, unique-value no-upsert enforcement, intermediate-db retry subsets, string and negative tempid refs including forward refs, non-upsert of newly allocated ref tempids, unique-ref numeric/lookup-ref upsert, unique cardinality-many upsert, conflicting unique-field subsets, and EDN text/prepared transaction coverage for ordinary identity upsert, lookup-ref upsert, retry convergence, forward ref tempids, and rollback on conflict covered; exact diagnostic text is Vev-shaped |
| `lookup_refs` | covered | Lookup refs covered in query entity/value positions, scalar and collection query inputs including mixed entity-id collections and multi-source joins, unresolved inline query lookup-ref errors, pull, tx entity/value/map-value/CAS entity/expected/value positions, nested tuple values, missing-ref retract no-ops, and `datoms`/`seek-datoms`/`rseek-datoms`/`index-range` index access including checked missing/non-unique lookup-ref errors; exact host/API rendering is Vev-shaped |
| `ident` | covered | DataScript query/transact/pull subset, entity ident lookup, and missing-ident retract no-ops covered; Vev returns refs as `Value.Entity` |
| `components` | covered | Component schema requires ref attrs, component `retractEntity`, `retractAttribute`, incoming-ref cleanup, forward pull/touch expansion, multival components, and reverse navigation covered; exact Clojure render/protocol shapes are host |
| `entity` | covered | Entity view API covered: id/db access, lookup-ref and ident construction, scalar/many attr reads, contains, missing/dangling refs, forward and reverse ref navigation including nested reverse navigation, and touch; Clojure map protocol/cache/print/equality semantics are host |
| `index` | covered | `datoms`, `find-datom` including empty DB and prefix lookups, `seek-datoms`, `rseek-datoms`, upstream `index-range` bounds/order examples, public AVET filtering, sequence-value compare cases, lookup-ref index args, and DataScript-shaped indexed-attribute plus unresolved lookup-ref errors covered; public access uses binary lower/upper bounds over sorted in-memory indexes; exact lazy sequence/API rendering is host shape |
| `validation` | covered | Runtime and EDN text validation cover bad entity ids, bad attrs, nil values, bad ref values, unknown ops, tempids outside add, bad tx root shapes, uniqueness, component schema, ident values, schema boolean attrs, schema keyword enum attrs, cardinality, symbol value types, and rollback/no-mutation; exact Clojure exception types/messages are host |
| `db` | covered | Immutable DB values, current datom indexes, public datom access, and semantic DB diff covered; Clojure hash/cache/record/uuid helpers are host/runtime-specific |
| `conn` | covered | `create-conn`, `conn-from-db`, `conn-from-datoms`, reset reports with before/after snapshots, tx-data, metadata, and named report-sink listener delivery covered; Clojure schema-map field shape is host |
| `filter` | covered | Materialized `filter-db` covers DataScript-style predicate filtering, chained filters, filtered entity reads, index-backed queries, and semantic DB equivalence; Clojure hash/equality/runtime wrapper behavior is host |
| `serialize` | covered | `init-db` from datoms plus typed serializable and EDN-ish datom snapshot text roundtrips covered, including schema, retractions, refs, symbol/map/vector values, special floats, and next-tx recovery; DataScript's JVM/CLJS codec matrix is host/format work |
| `issues` | covered | Engine-relevant regressions from `issues.cljc` are covered: vector result isolation, mixed-type DB diff, and schema inspection; Clojure metadata/pprint cases are host-specific |
| `listen` | covered | Named report-sink listeners cover registration, tx-data reports, metadata reports, and unlisten; arbitrary closure callbacks are host/ABI design work |
| `storage`, `datafy` | later | API/package features after semantic core |
| `tuples` | covered | Tuple attr schema, inferred tuple value type/cardinality metadata, final tuple schema validation independent of tx-data order, derived multi-tuple transaction maintenance, direct tuple attr rejection except redundant final-state tuple assertions, stale explicit direct tuple update rejection, unique tuple lookup refs including ref-component nested lookup refs, query lookup refs over tuple attrs in literal and EDN text APIs, tuple lookup-ref pull via literal/text/prepared APIs, tuple lookup-ref unique conflict/update paths through EDN text tx-data, component-based tuple upsert, direct tuple-value tempid upsert, tuple unique conflict and multi-component update shapes including DataScript's vector-sequential versus tx-map-atomic component updates, EDN text/prepared tx-data tuple component upsert, tuple lookup-ref entity refs, prepared direct tuple-value tempid upsert, conflict rollback, text/map tx-data direct tuple-value ignore cases, public AVET/index-range without explicit `:db/index`, tuple query functions, tuple type/attrs validation including invalid `:db/tupleAttrs` shapes, nested tuple dependency rejection, and cardinality-many tuple/component rejection covered; exact diagnostic text is Vev-shaped |

## Next Porting Order

The local compatibility suite currently passes 351 tests. The remaining
DataScript work should focus on the areas where Vev still differs in engine
semantics or required native interop, not on already-covered syntax families.

1. Tighten parser validation against the upstream parser namespaces. Query,
   pull, rule, return-map, and transaction text parsing are broad now, but
   malformed-shape handling still trails DataScript.
2. Extend the native callback ABI to listener/report callbacks and refine the
   final C ABI-facing registration shape for host adapters. Transaction
   function callbacks now work through the raw C ABI.
3. Use the new query profile counters to optimize recursive rules, large
   relations, aggregates, and index-backed planning before starting durable
   storage.
